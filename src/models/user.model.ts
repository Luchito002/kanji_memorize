export interface User {
  id: string;
  username: string;
  birthdate: string;
  created_at: string;
}

export interface UserMeResponse {
  id: string;
  username: string;
  birthdate: string;
  created_at: string;
}
